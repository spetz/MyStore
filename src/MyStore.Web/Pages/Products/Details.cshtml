@page "/products/{id:guid}"
@model DetailsModel
@{
}

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">@Model.Product.Name</h5>
                <h6 class="card-subtitle mb-2 text-muted">@Model.Product.Category</h6>
                <p class="card-text">@Model.Product.Price PLN</p>
            </div>
        </div>
    </div>
</div>